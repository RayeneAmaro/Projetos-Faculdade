int main () {
	int A, B, X;
	A = 10;
	B = 50;
	
	if (A != B){
		if (A > B) {	
		printf ("OK");
		} else {
		printf ("Nao");
		}
	}
	
return 0;

// If (Se) é uma função de comparação de 2 ou mais variaveis ; Else (então) se a definição do if não for valida.
// != diferente ; == igual ; === compara tudo, inclusive o tipo.
// Um = é atribuição ; Dois = (==) é comparação
}
